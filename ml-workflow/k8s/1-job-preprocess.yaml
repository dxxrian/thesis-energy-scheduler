# ml-workflow/kube/1-job-preprocess.yaml
apiVersion: batch/v1
kind: Job
metadata:
  name: ml-preprocess-job
  labels:
    app: ml-workflow # Hinzugefügt für das Cleanup
spec:
  template:
    metadata:
      labels:
        app: ml-workflow # Hinzugefügt für das Cleanup
    spec:
      restartPolicy: Never
      containers:
      - name: preprocess
        image: 192.168.178.136:5000/ml-workflow-cpu:v2
        command: ["python3", "preprocess.py"]
        volumeMounts:
        - name: data-storage
          mountPath: /data
      volumes:
      - name: data-storage
        persistentVolumeClaim:
          claimName: ml-workflow-pvc
  backoffLimit: 4
