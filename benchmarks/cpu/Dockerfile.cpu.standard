# Dockerfile.cpu.standard
FROM python:3.9-slim

WORKDIR /app

COPY cpu-task.py .

# Standard TensorFlow installieren (ist AVX optimiert)
RUN pip install --no-cache-dir tensorflow

ENTRYPOINT ["python", "cpu-task.py"]
